# Legacy files

`nythos_aquarium.py.deprecated` was an early Python/PyInstaller prototype
launcher from before the project moved to native C++ / DirectX 11. It
references an `aquarium` Python package that no longer exists in this repo.

Kept here for history only — not part of the build. Safe to delete entirely
in a future cleanup pass.
